// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    ext {
        gradle_plugin_version = '7.4.2'
        google_services_version = '4.3.10'
        kotlin_version = '1.8.0'

        appcompat_version = '1.6.1'
        media_version = '1.6.0'
        legacy_support_version = '1.0.0'
        multidex_support_lib_version = '2.0.1'

        firebase_auth_version = '22.3.0'
        firebase_firestore_version = '24.9.1'
        firebase_functions_version = '20.4.0'
        firebase_playintegrity_version = '17.1.1'
        firebase_appcheck_debug_version = '17.1.1'

        fastlane_screengrab_version = '2.0.0'

        victor_version = '1.1.2'
    }
    repositories {
        maven {
            url "https://plugins.gradle.org/m2/"
            name 'mvnrepository.com'
        }
        maven {
            url 'https://maven.google.com/'
            name 'Google'
        }
        google()
    }
    dependencies {
        classpath "com.android.tools.build:gradle:$gradle_plugin_version"
        classpath "com.google.gms:google-services:$google_services_version"
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }
}

allprojects {
    repositories {
        google()
        maven {
            url "https://plugins.gradle.org/m2/"
            name 'mvnrepository.com'
        }
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}

// REF: Parámetros compartidos: https://stackoverflow.com/a/20436423/5136913
ext {
    compileSdkVersion = 33
    minSdkVersion = 21
    targetSdkVersion = 33
}
